[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools"]

[project]
dependencies = [
  "pandas==2.3.3",
  "matplotlib==3.10.7"
]
authors = [{name = "Pierpaolo Iurato"}]
description = "Shared utilities for wine quality classification project"
name = "utils"
requires-python = ">=3.7"
version = "0.1.0"
