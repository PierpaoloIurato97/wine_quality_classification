[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools"]

[project]
authors = [{name = "Pierpaolo Iurato"}]
dependencies = [
  "torch==2.9.1+cpu",
]
description = "A model for wine quality classification using PyTorch."
name = "model"
requires-python = ">=3.7"
version = "0.1.0"
